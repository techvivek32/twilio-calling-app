{
  "name": "business-connect",
  "version": "1.0.0",
  "private": true,
  "description": "Business Connect — Flutter app + Next.js admin panel for a Twilio business line.",
  "scripts": {
    "dev": "npm run dev --prefix admin",
    "build": "npm run build --prefix admin",
    "start": "npm run start --prefix admin",
    "lint": "npm run lint --prefix admin",
    "seed": "npm run seed --prefix admin",
    "purge": "npm run purge --prefix admin",
    "e2e": "npm run e2e --prefix admin",
    "smoke": "npm run smoke --prefix admin --",
    "install:admin": "npm install --prefix admin"
  }
}
